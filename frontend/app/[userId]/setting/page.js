// app/[userId]/layout.js
import Header from "@/components/Header";

export default function UserLayout({  params }) {
  const { userId } = params;

  return (
    <div>
hello
         </div>
  );
}
